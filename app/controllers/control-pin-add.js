"use strict";

/*
    Provides the data and functionality to add pins to your boards. This will add data to firebase of the BoardId and the Users info.
 */
// app.controller("addPinCtrl", function($scope, pinFactory, $location, userFactory){
app.controller("addPinCtrl", function($scope, pinFactory, $location, userFactory) {

    $scope.buttonClick = function (){
    	// addUID();
    };
});